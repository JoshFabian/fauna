%ul
  %li.message-label{class: label.match(/inbox/i) ? 'active' : nil, :'data-label' => 'inbox'}
    = link_to '#' do
      %span.help-icon.inbox
        %i.icon.ion-android-inbox
      Inbox

  -# %li{class: label.downcase.match(/starred/) ? 'active' : nil}
  -#   = link_to user_messages_path(@user, 'starred') do
  -#     %span.help-icon.star
  -#       %i.icon.ion-star
  -#     Starred

  %li.message-label{class: label.match(/sent/i) ? 'active' : nil, :'data-label' => 'sentbox'}
    = link_to '#' do
      %span.help-icon.email
        %i.icon.ion-email
      Sent

  %li.message-label{class: label.match(/trash/i) ? 'active' : nil, :'data-label' => 'trash'}
    = link_to '#' do
      %span.help-icon.trash
        %i.icon.ion-ios7-trash
      Trash