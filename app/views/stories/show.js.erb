<% if @story.type == 'listing' %>
  <%= render(partial: "stories/story_listing", locals: {story: @story, listing: @object}) %>
<% elsif @story.type == 'post' %>
  <%= render(partial: "stories/story_post", locals: {story: @story, post: @object}) %>
<% end %>