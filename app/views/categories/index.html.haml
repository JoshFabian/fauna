.row
  .text-center
    %h5 Categories

  %table.tiny.small-12.columns
    %tr
      %th Id
      %th Name
      %th Slug
      %th Level
      %th Children
      %th Total Listings
      %th Active Listings

    - @categories.each do |category|
      %tr
        %td= category.id
        %td
          - if category.level == 1
            = link_to category.name, listing_category_path(category.slug)
          - else
            = category.name
        %td= category.slug
        %td= category.level
        %td= category.children_count
        %td= category.listings_count
        %td= category.listings.active.count
