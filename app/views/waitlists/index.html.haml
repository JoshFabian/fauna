.row
  .text-center
    %h3 #{@total_count} Beta Registrations
  .small-4.columns
    .text-center
      %h3 #{@buyers_count} Buyers
  .small-4.columns
    .text-center
      %h3 #{@sellers_count} Sellers
  .small-4.columns
    .text-center
      %h3 #{@both_count} Both

  %table.small-12.columns
    %tr
      %th Email
      %th Role

    - @waitlists.each do |waitlist|
      %tr
        %td= waitlist.email
        %td= waitlist.role
